here you can see how to INTENT futures with Kotlin for android apps,
there is small but so functional futures in,
INTENT OBJECT, sent to variables to next page, block to user manipulation,
variable usage, function usege, that content is for beginers and has explaintation on the
.kt pages.

you can find to .kt pages =>

 app -> src -> main -> java -> www -> fitpatients - fcom :

you must check to files first MainActivity.kt, secound BmrForm.kt,
lastone BmiResultScreen.kt

Design path is : app -> src -> res -> layout :

UNAL ZERAY
(CopyRight FitPatients.com)